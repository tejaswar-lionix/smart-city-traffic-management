from django.apps import AppConfig
class TrafficSignalsConfig(AppConfig):
    default_auto_field='django.db.models.BigAutoField'
    name='apps.traffic_signals'
    verbose_name='Adaptive signal control, Webster, phase timing, progression, coordination'